apiVersion: v1
kind: Service
metadata:
  name: cloudvibes-weather
  labels:
    app: cloudvibes-weather
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: cloudvibes-weather
